version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=production
      - OCTANE_SERVER=frankenphp
    restart: unless-stopped

  worker-ai:
    build:
      context: .
      dockerfile: Dockerfile.prod
    command: php artisan queue:work --queue=ai --timeout=120 --tries=3 --max-jobs=500 --max-time=3600
    environment:
      - APP_ENV=production
    restart: unless-stopped
    stop_grace_period: 130s

  worker-scraping:
    build:
      context: .
      dockerfile: Dockerfile.prod
    command: php artisan queue:work --queue=scraping,default --timeout=60 --max-jobs=1000 --max-time=3600
    environment:
      - APP_ENV=production
    restart: unless-stopped
    stop_grace_period: 130s

  scheduler:
    build:
      context: .
      dockerfile: Dockerfile.prod
    command: php artisan schedule:work
    environment:
      - APP_ENV=production
    restart: unless-stopped
